/** @type {import('next').NextConfig} */
const nextConfig = {
  // Hides the dev-mode "N" build-activity/issues badge (bottom-left corner).
  // Dev-only UI — has no effect on production builds either way.
  devIndicators: false,
};
export default nextConfig;
